import React from 'react';

const FilterBar = ({ onFilter }) => {
  // TODO: Implementar barra de filtros (fecha, tipo, categoría)

  return (
    <div className="filter-bar">
      <h3>Filtros</h3>
      {/* TODO: Agregar inputs de filtros */}
    </div>
  );
};

export default FilterBar;
